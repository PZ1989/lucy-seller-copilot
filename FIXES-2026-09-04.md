# Lucy Seller Copilot frontend checkpoint — 2026-09-04

This checkpoint restores the frontend to a clean source-only state for continued development.

Implemented in this checkpoint:
- Restored `/` as the Home dashboard.
- Restored `/billing` as the Subscription page.
- Rebuilt `/analytics` around real Etsy data currently available from the backend: active listings, views, favorites, and listing-level metrics.
- Kept Orders/Revenue unavailable instead of inventing values.
- Normalized Etsy listings/reviews responses from backend `{ items, total }` into frontend models.
- Fixed Etsy money mapping from `{ amount, currency }` and listing price rendering/editing.
- Preserved draft-only listing writes: create/update draft and draft image upload only.
- Kept physical/download draft payload mapping and shipping/readiness profile logic.
- Changed ChatGPT/Claude/Telegram display states so they are not falsely shown as connected.
- Made sidebar billing display depend on real billing status; no hardcoded active PRO state when billing is unavailable.
- Made Settings profile/shop/workspace use current session, Etsy connection, and billing data where available.
- Removed fake recent activity from Home and replaced it with real Etsy connection status.
- Removed stray duplicate root files created during earlier editing.
- Removed legacy unused listing form code.
- Removed render-time `Date.now()` hydration risks from mock research/automation sections by using stable reference data.
- Fixed internal Next.js navigation and legal refund link lint issue.

Validation:
- TypeScript: PASS (`tsc --noEmit`)
- ESLint: PASS

Not yet considered live backend functionality:
- Research
- Automation
- Reports
- AI chat actions that still use demo/mock content

Notes:
- `.env.local`, `.next`, `node_modules`, and build caches are intentionally excluded from this ZIP.
- On Windows run `npm install`, restore/create `.env.local`, then `npm run dev`.
